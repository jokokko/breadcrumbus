{
  "name": "breadcrumbus",
  "scripts": {
    "test": "karma start --single-run --browsers FirefoxHeadless karma.config.js",
    "lint": "eslint ./src/*.js",
    "pack": "web-ext build -s src/ -a dist/"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.7.2",
    "karma": "^1.7.1",
    "karma-spec-reporter": "^0.0.31",
    "karma-chai": "^0.1.0",
    "karma-firefox-launcher": "^1.0.1",
    "karma-mocha": "^1.3.0",
    "mocha": "^4.0.1",
    "angular-mocks": "^1.6.5"
  }
}
